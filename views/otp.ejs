<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify Your Account | Eventify</title>
    <link rel="stylesheet" href="/globals.css">
    <link rel="stylesheet" href="/signup.css"> <!-- സൈൻ-അപ്പ് പേജിലെ അതേ സ്റ്റൈൽ ഉപയോഗിക്കുന്നു -->
</head>
<body>
    <%- include('partials/loader') %>
    <div class="container">
        <div class="left-panel">
            <img src="https://i.gifer.com/embedded/download/OZpL.gif" alt="Event Crowd" class="bg-image" />
            <h1 class="eventify-title">EVENTIFY</h1>
        </div>
        <div class="right-panel">
            <div class="form-wrapper">
                <h1 class="form-title">EMAIL VERIFICATION</h1>
                <p class="form-subtext">We've sent a 6-digit OTP to <strong><%= email %></strong>. Please enter it below to verify your account.</p>
                
                <% if (error) { %>
                    <p style="color: red; text-align: center; margin-bottom: 1rem;"><%= error %></p>
                <% } %>

                <form action="/userauth/verify-otp" method="POST">
                    <input type="hidden" name="email" value="<%= email %>">
                    <input type="text" name="otp" placeholder="Enter 6-digit OTP" required maxlength="6">
                    <button type="submit">Verify Account</button>
                </form>

                <div class="login-link">
                    <p>Didn't receive the code? <a href="/userauth/resend-otp?email=<%= email %>">Resend OTP</a></p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
